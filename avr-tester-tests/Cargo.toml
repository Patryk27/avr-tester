[workspace]
members = [
    "components-shift-register",
    "pins-analog",
    "pins-digital",
    "timeout",
    "uart",
    "xx-bits",
    "xx-eval",
]

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "s"
panic = "abort"
